#specify mode of operation, i.e, terminate instances in autoscaling group or terminate clusters in ecs
# accepted values:1) autoscaling 2) ecs
mode_of_operation = ''


#Arn of ecs cluster where chaos need to be introduced
cluster_arn=''

#name of autoscaling group where chaos need to be introduced
autoscaling_group_name=''
